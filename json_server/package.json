{
  "name": "json_server",
  "version": "1.0.0",
  "description": "mock data",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json --port 8090 --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
